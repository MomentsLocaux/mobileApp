# Audit RLS — Moments Locaux (projet Supabase `prymkgkafaovhzopslea`)

Date : 2026-06-05 · Lecture seule + Lot 1 appliqué · Source de vérité : base prod.

## Méthode

`pg_class.relrowsecurity` (RLS on/off) croisé avec `pg_policies` pour chaque
table du schéma `public`. Rappel : **RLS désactivé = les policies sont inertes
et la table est totalement ouverte** aux rôles `anon` / `authenticated`.
Le scraper utilise `SUPABASE_SERVICE_ROLE_KEY` (bypass RLS).

## Lot 1 — APPLIQUÉ (migration `enable_rls_ingestion_tables`)

| Table | Avant | Après | Justification |
|---|---|---|---|
| `event_import_staging` | RLS off, 0 policy | RLS on | Aucune policy → verrouillée pour anon/auth. Scraper en service_role. Non lue par mobile/WebConsole. |
| `event_subcategory` | RLS off, SELECT public | RLS on | Lecture taxonomie conservée (policy SELECT anon+auth). Écritures anon/auth bloquées. |
| `event_tag` | RLS off, SELECT public | RLS on | Idem `event_subcategory`. |

Usages vérifiés : mobile (`taxonomyStore`) et scraper (`CategorizationService`)
ne font que des SELECT ; la WebConsole ne touche aucune des trois tables.
Activation **non bloquante**.

## Lot 2 — APPLIQUÉ (migration `secure_profiles_rls`) — **P0 résolu**

Voir `../migrations/20260605_secure_profiles_rls.sql`.
- Risque corrigé : RLS off → n'importe qui pouvait lire/écrire tous les profils
  (escalade `role`, contournement `status`/`ban_until`). C'était le risque majeur.
- Flux clients vérifiés : création via `auth-provider.ts > ensureProfile()`
  (`id = auth.uid()`, `role='particulier'`) ; onboarding via
  `OnboardingScreen.tsx` (UPDATE self, choix de rôle) ; invité via
  « Continuer en tant qu'invité » (`role='invite'`). Pas de trigger `handle_new_user`.
- Modèle appliqué :
  - `profiles_insert_self` : un user crée uniquement sa propre ligne (`id = auth.uid()`).
  - `profiles_update_own` : un user met à jour uniquement sa propre ligne.
  - Trigger `trg_profiles_privilege_guard` : pour un non-modérateur, `role`
    auto-attribuable parmi `particulier/professionnel/institutionnel/invite`
    uniquement ; `admin`/`moderateur` interdits ; `status`/`ban_until` non
    modifiables (forcés `active`/`null` à la création).
  - Bypass : modérateurs (`is_moderator()`) et service_role (`auth.uid()` null).
  - La policy existante `profiles_update_moderation` (modérateurs) reste active.

## Lot 3 — APPLIQUÉ (migration `enable_rls_remaining_tables`)

Tables hors-périmètre initiales, vérifiées **non référencées par nom** dans
mobileApp / scraper / WebConsole → activation sans risque et consistante.

| Table | Avant | Après | Décision |
|---|---|---|---|
| `contest_reports` | RLS off, 0 policy | RLS on, 0 policy | Service-only, consistant avec les autres `contest_*` (déjà RLS on / 0 policy). |
| `enum_types` | RLS off | RLS on + SELECT public | Référence ; lecture publique conservée, écritures verrouillées. |
| `enum_values` | RLS off | RLS on + SELECT public | Idem. |
| `enum_value_translations` | RLS off | RLS on + SELECT public | Idem (i18n). |
| `_migrations` | RLS off | RLS on, 0 policy | Registre interne, service-only. |

Après les Lots 1 à 3, **plus aucune table publique n'a RLS désactivé** (Lot 2 inclus).

## Note connexe (non sécurité, à vérifier fonctionnellement)

Plusieurs tables ont **RLS activé mais 0 policy** → totalement inaccessibles
aux clients anon/auth (lecture comprise), accès seulement via service_role /
fonctions `security definer` :
`active_boosts, activity_log, badges, contest_entries, contest_rewards,
contest_votes, contests, kv_store_b32ec4be, leaderboard, leaderboard_archive,
leaderboard_weekly, lumo_rules, lumo_transactions, missions, shop_items,
user_badges, user_inventory, user_levels, user_missions, user_preferences,
user_xp, xp, xp_rules`.

Ce n'est pas une faille (deny-by-default), mais si certaines de ces tables sont
censées être lues directement côté client (ex. `badges`, `shop_items`,
`leaderboard`, `user_preferences`), elles sont actuellement inaccessibles et
dépendent d'un accès serveur. À confirmer selon les besoins produit.

## Prochaines étapes proposées

1. Test fonctionnel du Lot 2 : sign-up, onboarding (choix pro/institutionnel),
   « Continuer en tant qu'invité » — vérifier que rien n'est cassé et qu'une
   tentative d'auto-attribution `admin`/`moderateur` échoue.
2. Revue fonctionnelle des tables « RLS on / 0 policy » réellement attendues côté client.
